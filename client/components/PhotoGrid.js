import React from 'react';
 
const PhotoGrid = React.createClass({
    render() {
        return (
            <div className="photo-grid">
            Hey, soy el PhotoGrid
            </div>
        );
    }
});

export default PhotoGrid;